<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Autorzy</title>
</head>
<body>
    <%- include('partials/navigation') %>
    <h1>Autorzy</h1>
    <% if (authors.length === 0) { %>
        <p>Nie znaleziono żadnych autorów</p>
    <% } else { %>
        <ul>
            <% authors.forEach(author => { %>
                <li><%= author.name %></li>
            <% }) %>
        </ul>
    <% } %>
</body>
</html>