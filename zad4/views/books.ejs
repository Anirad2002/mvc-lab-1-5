<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Książki</title>
</head>
<body>
    <% include('partials/navigation') %>
    <h1>Książki</h1>
    <% if (books.length === 0) { %>
        <p>Nie znaleziono żadnych książek</p>
    <% } else { %>
        <ul>
            <% books.forEach(book => { %>
                <li>
                    <%= book.title %> - <%= book.publishingYear %>
                    <form action="/book/delete/<%= book.id %>?_method=DELETE" method="POST">
                        <button type="submit">Usuń</button>
                    </form>
                    <a href="/book/<%= book.id %>">Szczegóły</a>
                </li>
            <% }) %>
        </ul>
    <% } %>
</body>
</html>